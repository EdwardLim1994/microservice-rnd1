extend schema @link(url: "https://specs.apollo.dev/federation/v2.14", import:["@key", "@shareable", "@external"])

type Query {
  demo2: Demo2
}

type Demo2 @key(fields: "id") {
  id: ID!
  name: String!
}

extend type Demo1 @key(fields: "id"){
  id: ID! @external
  demo2: [Demo2]
}
