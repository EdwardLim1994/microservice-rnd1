BUN_TARGET=bun-linux-x64

# gRPC
GRPC_PORT=5004

# GraphQL
GRAPHQL_PORT=4004

# Database (Vault-backed at runtime — see packages/server/CLAUDE.md's Database section).
# DATABASE_URL is still required by prisma.config.ts for Prisma CLI operations.
DATABASE_URL=postgresql://myuser:mypassword@localhost:5104/leave
DB_HOST=localhost
DB_PORT=5104
DB_NAME=leave
VAULT_ADDR=http://localhost:8200
VAULT_DB_ROLE=leave-role
