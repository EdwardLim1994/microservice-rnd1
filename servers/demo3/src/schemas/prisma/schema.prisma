generator client {
    provider = "prisma-client-js"
    output   = "../../generated/prisma"
}

datasource db {
    provider = "postgresql"
}

model Demo {
    id   String @id @default(cuid())
    name String
}
