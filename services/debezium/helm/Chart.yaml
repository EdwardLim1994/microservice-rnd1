apiVersion: v2
name: debezium
description: Kafka Connect (custom image, bundles the Debezium Postgres connector) — shared infra, deployed independently of any app
type: application
version: 0.1.0
appVersion: "8.2.2"
