# The "infra" Namespace itself is declared once, centrally, in services/Tiltfile — every chart
# here just targets it via namespace="infra" below.
k8s_yaml(helm("./helm", name="kafka", namespace="infra"))
k8s_resource("kafka", labels=["infra"])
