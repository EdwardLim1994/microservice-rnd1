# Placeholder — no subgraph servers exist yet (see ../../rover.yaml). Router will fail to start
# against this file as-is; run `bun run supergraph` once at least one subgraph is registered in
# rover.yaml, which overwrites this file with a real composed schema.
schema {
  query: Query
}

type Query {
  _placeholder: Boolean
}
