apiVersion: v2
name: apollo-router
description: Apollo Router, fronting the federation subgraphs — shared infra, deployed independently of any app
type: application
version: 0.1.0
appVersion: "2.15.0"
dependencies:
  - name: router
    version: "2.15.0"
    repository: "oci://ghcr.io/apollographql/helm-charts"
