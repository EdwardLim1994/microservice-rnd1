# Generated by `bun run supergraph` (packages/script/src/bin/compose-supergraph.ts) from
# ../rover.yaml — do not hand-edit, regenerate and commit the diff instead.
#
# PLACEHOLDER: server1-graphql/server2-graphql were removed and rover.yaml now has zero
# subgraphs — `rover supergraph compose` can't produce a real composed schema from an empty
# subgraph list. This is a minimal syntactically-valid schema (Apollo Router needs some file to
# mount) with no join__Graph entries. Regenerate for real once a new subgraph exists.
router:
  supergraphFile: |
    schema {
      query: Query
    }

    type Query {
      _empty: String
    }
