# Generated by `bun run supergraph` (packages/script/src/bin/compose-supergraph.ts) from
# ../rover.yaml — do not hand-edit, regenerate and commit the diff instead.
#
# Placeholder — no subgraph servers exist yet (see ../rover.yaml). Router will fail to start
# against this as-is; run `bun run supergraph` once a subgraph is registered in rover.yaml.
router:
  supergraphFile: |
    schema {
      query: Query
    }

    type Query {
      _placeholder: Boolean
    }
