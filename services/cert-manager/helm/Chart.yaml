apiVersion: v2
name: cert-manager
description: cert-manager — issues/renews TLS certs for Traefik via a self-signed Issuer (see services/cert-manager-config)
type: application
version: 0.1.0
appVersion: "v1.16.2"
dependencies:
  - name: cert-manager
    version: "v1.16.2"
    repository: "https://charts.jetstack.io"
