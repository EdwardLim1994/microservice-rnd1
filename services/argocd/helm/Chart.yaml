apiVersion: v2
name: argocd
description: ArgoCD — cluster tooling, prod-only (no Tiltfile, not deployed locally)
type: application
version: 0.1.0
appVersion: "10.2.1"
dependencies:
  - name: argo-cd
    version: "10.2.1"
    repository: "https://argoproj.github.io/argo-helm"
