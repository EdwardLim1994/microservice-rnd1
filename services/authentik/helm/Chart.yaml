apiVersion: v2
name: authentik
description: Authentik identity provider — shared infra, deployed independently of any app
type: application
version: 0.2.0
appVersion: "2026.5.6"
dependencies:
  - name: authentik
    version: "2026.5.6"
    repository: "https://charts.goauthentik.io"
