apiVersion: v2
name: authentik
description: authentik (server + worker, dedicated Postgres + Redis) — shared infra, deployed independently of any app, no integration yet
type: application
version: 0.1.0
appVersion: "2026.5.4"
