services:
  adminer:
    image: adminer:latest
    container_name: adminer
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - 8090:8080
    networks:
      - adminer

networks:
  adminer:
    driver: bridge
