apiVersion: v1
kind: Service
metadata:
  name: ca-distribution
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: ca-distribution
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ca-distribution
  ports:
    - name: http
      port: 80
      targetPort: http
