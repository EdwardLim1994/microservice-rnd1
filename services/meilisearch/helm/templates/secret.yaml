apiVersion: v1
kind: Secret
metadata:
  name: meilisearch-secret
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "meilisearch.labels" . | nindent 4 }}
stringData:
  MEILI_MASTER_KEY: {{ .Values.masterKey | quote }}
