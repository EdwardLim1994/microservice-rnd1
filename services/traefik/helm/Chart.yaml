apiVersion: v2
name: traefik
description: Traefik reverse proxy, fronting every browser-facing Ingress in the cluster — shared infra, deployed independently of any app
type: application
version: 0.1.0
appVersion: "3.3"
