apiVersion: v2
name: traefik
description: Traefik reverse proxy — shared infra, deployed independently of any app
type: application
version: 0.3.0
appVersion: "v3.1.2"
dependencies:
  - name: traefik
    version: "33.0.0"
    repository: "https://traefik.github.io/charts"
