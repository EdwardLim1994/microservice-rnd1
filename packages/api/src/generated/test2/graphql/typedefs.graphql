schema
  @link(
    url: "https://specs.apollo.dev/federation/v2.14"
    import: ["@key", "@shareable"]
  )
{
  query: Query
}

type Query {
  test2: Test2
}

type Test1 @key(fields: "id") {
  id: ID! @shareable
  test2: [Test2]
}

type Test2 @key(fields: "id") {
  id: ID!
}
