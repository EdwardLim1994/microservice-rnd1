apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: dev
ports:
  - port: 8080:30080
    nodeFilters:
      - server:0
  - port: 8443:30443
    nodeFilters:
      - server:0
options:
  k3s:
    extraArgs:
      - arg: --disable=traefik
        nodeFilters:
          - server:*
