apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: staging
ports:
  - port: 8083:30080
    nodeFilters:
      - server:0
  - port: 8446:30443
    nodeFilters:
      - server:0
options:
  k3s:
    extraArgs:
      - arg: --disable=traefik
        nodeFilters:
          - server:*
