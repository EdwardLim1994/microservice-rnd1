{
	"name": "{{ name }}",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"devDependencies": {
		"@types/bun": "latest"
	},
	"dependencies": {
		"server": "workspace:*",
		"config": "workspace:*"
	},
	"scripts": {
		"dev": "bun run index.ts --watch --verbose"
	}
}
