apiVersion: v2
name: leave-infra
description: leave's deploy-once infra (postgres) — split from leave's own app chart so it deploys once via Terraform instead of being re-applied on every app-chart deploy.
type: application
version: 0.1.0
appVersion: "0.1.0"
