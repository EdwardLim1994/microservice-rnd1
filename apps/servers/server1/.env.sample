BUN_TARGET=bun-linux-x64

# Database — static superuser creds (testing server, no Vault indirection). Also read directly
# by prisma.config.ts for Prisma CLI operations (generate/migrate).
DATABASE_URL=postgresql://myuser:mypassword@localhost:5101/server1
DB_HOST=localhost
DB_PORT=5101
DB_NAME=server1
